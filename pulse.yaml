services:
  bifrost:
    path: transports/bifrost-http
    build: sh -c 'if [ -n "$DEBUG" ]; then go build -tags dev -gcflags="all=-N -l" -o tmp/main .; else go build -tags dev -o tmp/main .; fi'
    run: sh -c 'if [ -n "$DEBUG" ]; then dlv exec ./tmp/main --listen=127.0.0.1:2345 --headless=true --api-version=2 --accept-multiclient --log -- -host $HOST -port $PORT -log-style $LOG_STYLE -log-level $LOG_LEVEL ${PROMETHEUS_LABELS:+-prometheus-labels "$PROMETHEUS_LABELS"} ${APP_DIR:+-app-dir "$APP_DIR"}; else ./tmp/main -host $HOST -port $PORT -log-style $LOG_STYLE -log-level $LOG_LEVEL ${PROMETHEUS_LABELS:+-prometheus-labels "$PROMETHEUS_LABELS"} ${APP_DIR:+-app-dir "$APP_DIR"}; fi'
    env:
      BIFROST_UI_DEV: "true"
    watch: [".go", "go.mod", "go.sum"]
    ignore: ["*_test.go"]
    kill_timeout: 10s
    proxy:
      addr: ":${PORT}"
    healthcheck:
      path: /health
      interval: 2s
      timeout: 120s
